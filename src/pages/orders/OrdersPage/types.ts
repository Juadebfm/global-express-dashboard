// This module's contents live in `src/pages/shared/orderStatus.ts` — it's
// shared with the consolidated Operations workspace. Re-exported here so
// existing relative imports (`./types`, `../types`) across this directory
// keep working unchanged.
export * from '@/pages/shared/orderStatus';
